{
  "name": "vibe-connect-root",
  "version": "1.0.0",
  "private": true,
  "description": "Vibe Connect Root Workspace",
  "scripts": {
    "start": "npm start --prefix vibe-frontend",
    "frontend": "npm start --prefix vibe-frontend",
    "backend": "npm run dev --prefix vibe-backend",
    "build": "npm run build --prefix vibe-frontend"
  }
}
